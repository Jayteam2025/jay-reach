import { SqueletteListe } from '../../squelette';

/** Réglage avancé : l'URL du webhook et son secret. */
export default function Chargement() {
  return <SqueletteListe active="providers" cartes={2} />;
}
